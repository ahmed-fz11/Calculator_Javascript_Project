const variables = {
    e:Math.E,
    pi:Math.PI,
    π:Math.PI,
}; //variable object where all variables will be stored. e and pi/π already stored

export {variables};
// module.exports = variables;

